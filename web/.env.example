# Base URL of the backend API.
# In development you can leave this blank — Vite proxies "/api" to the backend
# (see vite.config.ts), so the cookie stays same-origin.
# In production, set it to your deployed API origin, e.g. https://api.example.com/api
VITE_API_URL=
